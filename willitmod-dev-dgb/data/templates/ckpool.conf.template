{
  "btcd": [
    {
      "url": "dgbd:${DGB_RPC_PORT}",
      "auth": "${RPC_USER}",
      "pass": "${RPC_PASSWORD}",
      "notify": true
    }
  ],
  "btcaddress": "${PAYOUT_ADDRESS}",
  "btcsig": "/mined by WillItMod on Umbrel/",
  "mindiff": 1024,
  "startdiff": 1024,
  "maxdiff": 0,
  "logdir": "/www",
  "webdir": "/www/pool",
  "userdir": "/www/users",
  "zmqblock": "tcp://dgbd:${DGB_ZMQ_HASHBLOCK_PORT}"
}
