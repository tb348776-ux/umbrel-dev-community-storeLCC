{
  "btcd": [
    {
      "url": "bitcoind:${BTC_RPC_PORT}",
      "auth": "${RPC_USER}",
      "pass": "${RPC_PASSWORD}",
      "notify": true
    }
  ],
  "auxd": [
    {
      "url": "fractald:${FB_RPC_PORT}",
      "auth": "${FB_RPC_USER}",
      "pass": "${FB_RPC_PASSWORD}"
    }
  ],
  "auxaddress": "${FB_PAYOUT_ADDRESS}",
  "btcaddress": "${PAYOUT_ADDRESS}",
  "btcsig": "/AxeBTCF/",
  "mindiff": 128,
  "startdiff": 512,
  "maxdiff": 131072,
  "logdir": "/www",
  "webdir": "/www/pool",
  "userdir": "/www/users",
  "zmqblock": "tcp://bitcoind:${BTC_ZMQ_HASHBLOCK_PORT}"
}
